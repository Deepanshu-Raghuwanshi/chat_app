{
  "name": "notification-service",
  "tags": ["layer:infrastructure", "type:app"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/apps/notification-service"],
      "options": {
                  "command": "pnpm tsc -p tsconfig.notification-service.json && pnpm tsc-alias -p tsconfig.notification-service.json" 
      }
    },
    "lint": {},
    "test": {},
    "typecheck": {}
  }
}
