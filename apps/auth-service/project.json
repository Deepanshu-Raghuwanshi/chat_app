{
  "name": "auth-service",
  "tags": ["layer:infrastructure", "type:app"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/apps/auth-service"],
      "options": {
        "command": "pnpm tsc -p tsconfig.auth-service.json && pnpm tsc-alias -p tsconfig.auth-service.json"
      }
    },
    "lint": {},
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx cross-env TS_NODE_PROJECT=tsconfig.test.json npx mocha -r ts-node/register/transpile-only -r tsconfig-paths/register apps/auth-service/tests/**/*.spec.ts --timeout 15000"
      }
    },
    "typecheck": {},
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cross-env TS_NODE_PROJECT=tsconfig.auth-service.json ts-node-dev -r tsconfig-paths/register --respawn apps/auth-service/src/main.ts"
      }
    }
  }
}
